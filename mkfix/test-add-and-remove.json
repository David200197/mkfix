[
  {
    "path": "example/index.js",
    "changes": [
      {
        "type": "remove",
        "line": 9,
        "old_code": "const MAX_RETRIES = 3;"
      },
      {
        "type": "add",
        "line": 8,
        "new_code": "const MAX_RETRIES = parseInt(process.env.MAX_RETRIES) || 3;\nconst RETRY_DELAY = parseInt(process.env.RETRY_DELAY) || 500;"
      }
    ]
  }
]
